export const FEATURES = {
  /**
   * Real Hospitable availability data (central hub for Airbnb, VRBO, direct).
   * When false, AvailabilityCalendar shows mock seeded data.
   */
  HOSPITABLE_LIVE: true,
} as const;
